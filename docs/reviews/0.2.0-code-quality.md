# 0.2.0 Code-quality and Security Review

Status: PASS. Independent code-quality/security rereview found no remaining
actionable issues after the fixes below.

## Evidence

- Branch: `feat/governed-adaptation-and-harness-evolution`
- Task 17 base: `29342b17`
- Main CodeRabbit remediation: `b08d343`
- Protected-worker startup fix: `5efcc48`
- Handbook review refresh: `54740d3`
- Successful wheel CLI smoke fix: `5885d6c`
- Evidence-path privacy fix and branch source: `45059e8`
- Verification source: `d99699c`; its tree
  (`4033f309eaef254d828ce9b12d7d45d31c4c1a6f`) is byte-identical to
  branch source `45059e8`.
- CI timeout correction: `17934d0`
- Protected-field security fix and current handbook source: `d720d01`
- Authoritative nine-check generated Windows-byte SHA-256:
  `bc82eaaecedd5c96e38b8d3b9e4e3fcb746223541b0d114e9d0d4b1025ebe3ca`
- Authoritative committed LF-normalized SHA-256:
  `50fb38c1045d2bac2e76db5f0e979701fa12a82e30dbfaaf05ce61b20aaa31d0`
- Result: all nine checks passed; `2027 passed, 11 skipped`; 91.14% branch
  coverage.
- Final compact migration/e2e/handbook shard: 58 passed.
- Reviewer-focused rereview suite: 57 passed.
- Broader targeted regressions after the authority fix: 449 passed.

The gate covers formatting, lint, strict types, tests, Bandit, dependency
audit, build, package validation, and isolated wheel installation.

## Findings and dispositions

- Protected evaluation outputs are capped on the exact wire representation
  before persistence, and request counters have an explicit bound.
- CLI failures return stable sanitized envelopes for validation, JSON,
  database, storage, operating-system, and unexpected errors without exposing
  internal exception text.
- Imported-pattern scanning covers additional text/source suffixes, aggregates
  split declarations across full Python modules and `.pyi` stubs, detects
  structured schema/path aliases, and retains false-positive regression tests.
- Wheel validation rejects unsafe paths, `.pth` files, symlink members, and
  unexpected entry points.
- The wheel smoke uses a clean environment with
  `pip install --no-index --no-deps` for only the project wheel. Its
  standard-library bootstrap returns an exit-0, validated JSON version
  envelope and lazily imports runtime dependencies only for normal execution.
- Actor independence now rejects identical actor IDs and any shared non-null
  provider, model, adapter, or configuration identity. Optional configuration
  hashes are not required merely to be present.
- Measurement schema v2 requires explicit unmeasured coverage gaps while
  approved schema-v1 evidence remains readable, canonical, and immutable.
- Artifact and nested wheel-smoke paths are sanitized case-insensitively on
  Windows before review evidence is persisted.

## CodeRabbit disposition

The review contained 15 major threads plus 30 minor findings. Forty-three valid
or partially valid findings were fixed. Two suggestions were not applied
because verification showed that they would be incorrect:

- V2 remains the active authority for a V2-to-V1 rollback under the approved
  policy-transition design.
- `# nosec B105` is the correct Bandit suppression for an intentional fixture;
  Ruff S105 is not selected, and the complete Ruff checks pass.

## Transparent failed attempts

The release record preserves, but does not elevate:

- the first wheel-dependency attempt,
  `7ec794c53e81621b8106a6c23d0153b5d5cb8611061f7ea99b5f205c67a40fb0`
  (committed LF:
  `1d552cdf222903278a7b7f10d630c864ce7275ca9e9f9c4b9a299c86ec8dc424`);
- the pre-review nine-check success,
  `c947781f3062405d77a62d6d6bdba25d05f27cac30a92091e69fd978f5d7fb42`
  (committed LF:
  `5df28c5fc0723941078834facc4b61c60fd171c556f84a1a3a9a35ca3b2cc981`);
  and
- the over-strict identity attempt,
  `e20cad5bf2c6711fb2fd330941d30b46b1a3427e469a8552696bbaad835ba9c5`
  (committed LF:
  `af9ea953689d319b9d6ca3aa3385639c7e56015be7433d98f5899e400e2d7d56`).

The last attempt failed 72 tests and stopped five downstream checks. The
corrected source was independently rereviewed and then exercised by the final
authoritative nine-check run.

No user/global Git configuration was changed. The managed-worktree ownership
exception was process-local and restricted to the exact worktree.
